//Latihan if

let language = "English"; //deklarasi variabel language
let greeting = "Selamat Pagi"; //deklarasi variabel greeting (default)

if(language === "English"){ //jika language sama dengan "English"
    greeting = "Good Morning"; //Maka variabel greeting akan berubah menjadi "Good Morning"
}
console.log(greeting); //mencetak variabel greeting.